console.log('Explore JQuery');
var q = $('ul.people').addClass('hello').removeClass('people')
console.log(q);

// METHOD CHAINING - calling one method after another, 
// and each method affects the parent object